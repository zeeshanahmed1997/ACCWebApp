@{
    ViewData["Title"] = "Home Page";

}

@*@await Html.PartialAsync("~/Views/Miscellaneous/SlideShow.cshtml")*@
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Page with Filters</title>
    <link rel="stylesheet" href="~/css/Index.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        .productImage {
            height: 100px;
            width: 100px;
        }
        .filter-section {
            /* Adjust the padding or margin values as needed */
            align-content:start;
        }
        .filter label {
            display:inline; /* Display radio labels as block-level elements */
        }
        h1{
            color:red;
            border-block-style:dotted;
        }
    </style>
</head>
<body>
    <h1>@ViewData["Title"]</h1>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-3 col-lg-2 filter-section">
                <div class="filter">
                    <h3>Color:</h3>
                    <div class="radio-group">
                        <label><input type="radio" class="color-filter" name="color" data-color="red"> Red</label>
                        <label><input type="radio" class="color-filter" name="color" data-color="blue"> Blue</label>
                        <label><input type="radio" class="color-filter" name="color" data-color="green"> Green</label>
                    </div>
                </div>
                <div class="filter">
                    <h3>Color:</h3>
                    <div class="radio-group">
                        <label><input type="radio" class="color-filter" name="color" data-color="red"> Red</label>
                        <label><input type="radio" class="color-filter" name="color" data-color="blue"> Blue</label>
                        <label><input type="radio" class="color-filter" name="color" data-color="green"> Green</label>
                    </div>
                </div>
                <div class="filter">
                    <h3>Color:</h3>
                    <div class="radio-group">
                        <label><input type="radio" class="color-filter" name="color" data-color="red"> Red</label>
                        <label><input type="radio" class="color-filter" name="color" data-color="blue"> Blue</label>
                        <label><input type="radio" class="color-filter" name="color" data-color="green"> Green</label>
                    </div>
                </div>
            </div>
            <div class="col-md-9">
                <div class="row">
                    <div class="col-md-4 col-auto">
                        <div class="product">
                            <img class="productImage" src="~/images/logo.png" alt="Product 1">
                            <h3>Product 1</h3>
                            <p>$19.99</p>
                            <button class="btn btn-secondary btn-block w-50">Add to Cart</button>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="product">
                            <img class="productImage" src="~/images/logo.png" alt="Product 1">
                            <h3>Product 1</h3>
                            <p>$19.99</p>
                            <button class="btn btn-secondary btn-block">Add to Cart</button>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="product">
                            <img class="productImage" src="~/images/logo.png" alt="Product 1">
                            <h3>Product 1</h3>
                            <p>$19.99</p>
                            <button class="btn btn-secondary btn-block">Add to Cart</button>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="product">
                            <img class="productImage" src="~/images/logo.png" alt="Product 1">
                            <h3>Product 1</h3>
                            <p>$19.99</p>
                            <button class="btn btn-secondary btn-block">Add to Cart</button>
                        </div>
                    </div>
                    <!-- Repeat similar code for other products -->
                </div>
            </div>
        </div>
    </div>

    <!-- Add your JavaScript code for filtering and rendering products here -->

</body>
</html>






















































@*<h2>Upload Video</h2>

    <form id="uploadForm" asp-controller="Home" asp-action="Upload" method="post" enctype="multipart/form-data">
        <input type="file" name="videoFile" />
        <input type="submit" value="Upload" />
    </form>

    <div id="progressDiv" style="display: none;">
        <progress id="progressBar" value="0" max="100"></progress>
        <span id="status">0%</span>
    </div>

    @section scripts {
        <script>
            // Function to display the progress bar and update progress status
            function showProgress() {
                document.getElementById('progressDiv').style.display = 'block';
            }

            // Function to update the progress bar
            function updateProgress(percent) {
                document.getElementById('progressBar').value = percent;
                document.getElementById('status').innerText = percent + '%';
            }

            // Attach an event listener to the form submission
            document.getElementById('uploadForm').addEventListener('submit', function (event) {
                event.preventDefault(); // Prevent default form submission
                showProgress(); // Show progress bar

                // Create FormData object to hold the form data
                var formData = new FormData(event.target);

                // Create a new XMLHttpRequest to handle the AJAX request
                var xhr = new XMLHttpRequest();

                // Attach an event listener to track the upload progress
                xhr.upload.addEventListener('progress', function (event) {
                    if (event.lengthComputable) {
                        var percent = (event.loaded / event.total) * 100;
                        updateProgress(Math.round(percent));
                    }
                });

                // Attach an event listener to handle the AJAX response
                xhr.onreadystatechange = function () {
                    if (xhr.readyState === XMLHttpRequest.DONE) {
                        if (xhr.status === 200) {
                            var response = JSON.parse(xhr.responseText);
                            // Handle the response, e.g., show a success message
                            alert(response.message);
                        } else {
                            // Handle the error, e.g., show an error message
                            alert('Error uploading the file.');
                        }
                        // Hide the progress bar after the response is received
                        document.getElementById('progressDiv').style.display = 'none';
                    }
                };

                // Open the AJAX request and send the form data
                xhr.open('POST', '@Url.Action("Upload", "Home")');
                xhr.send(formData);
            });
        </script>
    }*@

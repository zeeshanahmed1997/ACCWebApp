@{
    var userName = Context.Session.GetString("UserName");
    var userEmail = Context.Session.GetString("UserEmail");
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - MoonClothHouse</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/_Layout.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/MoonClothHous.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    @*<style>

    </style>*@
</head>

<body>
    <div class="col-md-12 brand-name">
        <a class="navbar-brand" asp-controller="Products" asp-action="ProductsLandingPage">MCH</a>
        <div class="d-flex justify-content-end">
            <div class="custom-navbar">
                <div class="nav-item">
                    @if (!string.IsNullOrEmpty(userName))
                    {
                        <div class="nav-item dropdown-toggle" id="userDropdown"  data-bs-toggle="dropdown"  aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-user" style="margin-right:5px;"></i>
                            @userName
                        </div>
                        <div class="dropdown-menu" aria-labelledby="userDropdown">
                            <a class="dropdown-item" href="#">User Information</a>
                            <form method="post" action="@Url.Action("Logout", "Accounts")">
                                <button type="submit" class="dropdown-item">Logout</button>
                            </form>
                        </div>
                    }
                    else
                    {
                        <button class="nav-item" id="loginButton" onclick="location.href='@Url.Action("Login", "Accounts")'">
                            <i class="fas fa-user" style="margin-right:5px;"></i> Login
                        </button>
                    }
                </div>
                <button class="nav-item" onclick="location.href='@Url.Action("Contact", "Home")'">
                    <i class="fa fa-phone" style="margin-right:5px;"></i> Contact
                </button>
                <button class="nav-item" onclick="location.href='@Url.Action("Cart", "Home")'">
                    <i class="fas fa-shopping-cart" style="margin-right:5px;"></i>
                    <span id="cartCount" class="cart-count">0</span>
                </button>
            </div>
        </div>
    </div>

    <header>
        <nav class="navbar navbar-expand-md gradient-custom-2">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                        data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <!-- Your existing dropdown menu code -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                               data-bs-toggle="dropdown" aria-expanded="false">
                                Men
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item" href="#">Cotton</a></li>
                                <li><a class="dropdown-item" href="#">Washing Wear</a></li>
                                <li><a class="dropdown-item" href="#">Soft Cotton</a></li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li><a class="dropdown-item" href="#">Something else here</a></li>
                            </ul>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                               data-bs-toggle="dropdown" aria-expanded="false">
                                Women
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item" href="#">Liner</a></li>
                                <li><a class="dropdown-item" href="#">Lawn</a></li>
                                <li><a class="dropdown-item" href="#">Khadder</a></li>
                                <li><a class="dropdown-item" href="#">Cotton</a></li>
                                <li><a class="dropdown-item" href="#">Viscose</a></li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li><a class="dropdown-item" href="#">Something else here</a></li>
                            </ul>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                               data-bs-toggle="dropdown" aria-expanded="false">
                                Kids
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item" href="#">Liner</a></li>
                                <li><a class="dropdown-item" href="#">Lawn</a></li>
                                <li><a class="dropdown-item" href="#">Khadder</a></li>
                                <li><a class="dropdown-item" href="#">Cotton</a></li>
                                <li><a class="dropdown-item" href="#">Viscose</a></li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li><a class="dropdown-item" href="#">Something else here</a></li>
                            </ul>
                        </li>
                        <!-- End of existing dropdown menu code -->

                    </ul>
                </div>
                <form class="d-flex">
                    <input class="form-control me-2" style="color:black" type="search" placeholder="Search for products" aria-label="Search">
                    <button class="btn btn-outline-light" type="submit"><i class="fas fa-search"></i></button>
                </form>
            </div>
        </nav>
        @if (TempData["LogoutSuccess"] != null)
        {
            <div id="logoutSuccessMessage" class="alert alert-success">
                You have been successfully logged out.
            </div>
        }
    </header>
    <section style="margin-bottom:200px;">
        <div class="flex">
            <main role="main">
                @RenderBody()
            </main>
        </div>
    </section>
    <footer class="footer">
        <div>
            <p>123 Main Street, City, Country</p>
            <p>Contact: +123456789</p>
        </div>
        <div>
            <p>&copy; 2024 Your Website Name. All Rights Reserved.</p>
        </div>
    </footer>
    <script>
        $(document).ready(function () {
            var logoutSuccessMessage = $("#logoutSuccessMessage");
            if (logoutSuccessMessage.length) {
                // Set a timeout to hide the message after 5 seconds
                setTimeout(function () {
                    logoutSuccessMessage.fadeOut("slow", function () {
                        logoutSuccessMessage.remove(); // Remove the element from the DOM
                    });
                }, 5000); // 5000 milliseconds = 5 seconds
            }
            $("#loginButton").click(function () {
                window.location.href = "@Url.Action("Login", "Accounts")";
            });
            function updateCartCount(count) {
                $('#cartCount').text(count);
            }
            function deleteCartCount() {
                localStorage.removeItem('cartCount');
                updateCartCount(0); // Update cart count to 0 in UI
            }
            // Check if cart count is stored in local storage
            var cartCount = localStorage.getItem('cartCount');

            // If cart count is not null or undefined, use its value, else default to 0
            if (cartCount !== null && cartCount !== undefined) {
                // Update cart count from local storage
                updateCartCount(parseInt(cartCount));
                setTimeout(function () {
                    deleteCartCount();
                }, 1 * 60 * 1000); // 2 minutes in milliseconds

            } else {
                // Default cart count to 0 if not found in local storage
                updateCartCount(0);
            }
        });
    </script>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>

</html>
